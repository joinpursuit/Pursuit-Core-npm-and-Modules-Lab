//sum function 
const sum = (a, b) => a + b;

//multiply function
const multiply = (a, b) => a * b;

//divide function
const divide =(a,b)=> (a / b);

//square function
const square = (num) => num * num;


module.exports = {
    sum, 
    multiply,
    divide,
    square
}
