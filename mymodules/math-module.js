const sum = (a, b) => a + b;

const multiply = (a, b) => a * b;

const divide = (a, b) => a / b;

const square = (n) => {
    return Math.pow(n, 2);
}

module.exports = {
    sum,
    multiply,
    divide,
    square,
}