const sum = (a, b) => a + b;

const multiply = (a, b) => a * b;

const divide = (a, b) => a/b;

const square = (a) => Math.sqrt(a)

module.exports ={
    sum: sum,
    multiply: multiply,
    divide: divide,
    square: square
}